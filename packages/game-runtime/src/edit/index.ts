/**
 * Modo edición del runtime (ticket 3.1): eventos de puntero de la escena y
 * palette derivada del pack. Puro (sin Phaser): la escena vive en
 * `@escaperoom/game-runtime/phaser` con `mode: 'edit'`.
 */
export * from "./events";
export * from "./palette";

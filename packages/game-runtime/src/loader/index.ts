/**
 * Loader puro del `RoomPackage` (sin Phaser): valida el JSON con los esquemas
 * de 0.6 y lo proyecta a un modelo listo para el runtime. Importable en Node y
 * en el navegador, así como en tests sin infraestructura.
 */
export * from "./types";
export * from "./errors";
export * from "./load";

/**
 * Pistas y diálogos (specs/05 §3, specs/08 §2.3): lógica pura sobre `HintDef` y
 * `DialogDef` del `RoomPackage`, con resolución de `LocalizedText` por idioma y
 * fallback. La consume el servidor (Colyseus) y los componentes de `web`.
 */
export * from "./localized-text";
export * from "./hints";
export * from "./dialogs";

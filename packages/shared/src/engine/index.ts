/**
 * Motor de reglas declarativo v1 (specs/05). Librería pura y determinista:
 * consume los esquemas de reglas del contrato `RoomPackage` (0.6) y expone una
 * API testeable sin red, base de datos ni `setTimeout`.
 */
export * from "./types";
export * from "./state";
export * from "./engine";

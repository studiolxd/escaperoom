{
  "name": "@escaperoom/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Procesos de cola del backend: consume la cola de analítica de Redis y escribe en analyticsEvent vía Prisma (specs/16, 14 §8), el job repetitivo de caducidad de claves (specs/02 §4.3), los envíos de invitación por email (specs/02 §4.4) el PDF de tarjetas-clave (ticket 5.7) y el job mensual de particiones y purga a 24 meses de analyticsEvent (ticket 6.11).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "NODE_ENV=development tsx watch src/main.ts",
    "start": "NODE_ENV=production tsx src/main.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@escaperoom/env": "workspace:*",
    "@escaperoom/kit": "workspace:*",
    "@escaperoom/shared": "workspace:*",
    "@sentry/node": "^10.69.0",
    "bullmq": "^6.3.8",
    "cron-parser": "5.10.1",
    "ioredis": "^6.0.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@escaperoom/config": "workspace:*",
    "@types/node": "^26.6.2",
    "eslint": "^10.11.0",
    "pdf-lib": "^1.17.1",
    "tsx": "^4.23.15",
    "typescript": "^6.0.3",
    "vitest": "^5.0.1"
  }
}
